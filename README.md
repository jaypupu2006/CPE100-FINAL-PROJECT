# วิธีคอมไพล์และรันโปรแกรม C หลายไฟล์ (Windows PowerShell)

## 🧩 ขั้นตอนการใช้งาน

### 1. เปิด PowerShell
ไปที่โฟลเดอร์ที่เก็บไฟล์โปรเจกต์ เช่น  
```
C:\Users\USER\Desktop\cpe100\batminton_final_project
```
หรือใช้คำสั่งเปลี่ยนโฟลเดอร์:
```bash
cd "C:\Users\USER\Desktop\cpe100\batminton_final_project"
```

---

### 2. คอมไพล์ไฟล์ทั้งหมดรวมกันเป็นไฟล์เดียว
พิมพ์คำสั่ง:
```bash
gcc main.c menu1.c -o badminton
```
#### 🔹 อธิบายคำสั่ง
- `gcc` = ตัวคอมไพล์ภาษา C  
- `main.c` และ `menu1.c` = ไฟล์โค้ดต้นฉบับ  
- `-o badminton` = ตั้งชื่อไฟล์รันเป็น badminton.exe

หลังรันคำสั่งนี้จะได้ไฟล์ **badminton.exe** อยู่ในโฟลเดอร์เดียวกัน

---

### 3. รันโปรแกรม
ใช้คำสั่ง:
```bash
.adminton.exe
```
> ต้องมี `.\` เพราะ PowerShell ต้องระบุว่าไฟล์นี้อยู่ในโฟลเดอร์ปัจจุบัน

---

## 🧠 หมายเหตุ
- ถ้ามีไฟล์อื่น เช่น `member.c`, `game.c` เพิ่มเข้าไปได้เลย เช่น
  ```bash
  gcc main.c menu1.c member.c game.c -o badminton
  ```
- ถ้าใช้ Command Prompt (cmd) ให้ใช้คำสั่ง:
  ```bash
  badminton.exe
  ```

---

💡 **Tip:**  
หากต้องการให้คอมไพล์ไฟล์ .c ทุกไฟล์ในโฟลเดอร์โดยอัตโนมัติ สามารถใช้คำสั่งนี้ได้เลย:
```bash
gcc *.c -o badminton
```
