# 🏸 Badminton Group Payment System

ระบบจัดการข้อมูลก๊วนแบดมินตัน – ภาษา C
โปรเจกต์นี้สร้างขึ้นเพื่อฝึกใช้ **File I/O**, **Struct**, และ **Dynamic Memory Allocation** โดยแบ่งระบบออกเป็น 3 ส่วนหลัก

---

## 📂 โครงสร้างไฟล์

```
📁 badminton-group-payment/
│
├─ main.c
├─ menu1.c / menu1.h      → ระบบสมาชิก
├─ menu2.c / menu2.h      → ระบบเปิดก๊วน (บันทึกรายวัน)
├─ menu2_impl.c           → ฟังก์ชันย่อยของเมนู 2
├─ menu3.c / menu3.h      → ระบบสรุปข้อมูล
│
├─ member.txt             → รายชื่อสมาชิก
├─ OSpayment.txt          → บันทึกยอดค้างชำระ
├─ config.txt             → เก็บราคาลูกขนไก่ / ค่าสนาม
│
├─ 📁 input/               → ไฟล์รายวัน เช่น `07-11-2025.txt`
└─ 📁 output/              → ไฟล์สรุป เช่น `07-11-2025.summary.full.txt`
```

---

## ⚙️ วิธีคอมไพล์

ใช้คำสั่งนี้ใน Terminal / CMD:

```bash
gcc main.c menu1.c menu2.c menu2_impl.c menu3.c -o badminton
```

หลังจากคอมไพล์สำเร็จ จะได้ไฟล์ `badminton.exe`
จากนั้นรันด้วยคำสั่ง:

```bash
./badminton
```

---

## 🧩 เมนูหลักของโปรแกรม

### 🔹 1. ระบบสมาชิก (`menu1.c`)

* `1. ลงทะเบียนสมาชิกก๊วน`
  → กรอกชื่อ-นามสกุล, ชื่อเล่น, เพศ
  → โปรแกรมจะเพิ่มข้อมูลลงใน `member.txt` พร้อมรันเลขสมาชิกอัตโนมัติ
* `2. ค้นหารายชื่อในระบบ`
  → ค้นหาได้ 3 แบบ: รหัส, ชื่อเล่น, ชื่อเต็ม
* `3. แสดงรายชื่อสมาชิกทั้งหมด`
  → แสดงข้อมูลในรูปตาราง

**โครงสร้างไฟล์ `member.txt`**

```
id|fullname|nickname|gender|created_at
1|สมชาย ใจดี|ชาย|1|2025-11-07
2|ปวีณา มณีศรี|ฝน|2|2025-11-07
```

---

### 🔹 2. เปิดก๊วน / แก้ไขข้อมูลรายวัน (`menu2.c`)

* **เปิดไฟล์รายวันอัตโนมัติ**
  → กรอกวันที่ เช่น `07-11-2025`
  → ถ้าไม่มีไฟล์จะสร้างใหม่อัตโนมัติใน `input/`
* **เมนูย่อย**

  1. ลงชื่อเบิกลูก

     * กรอกจำนวนผู้เล่น
     * เลือกสมาชิกจาก `member.txt` (ค้นหาตามรหัส/ชื่อเล่น/ชื่อเต็ม)
     * บันทึกจำนวนลูกและยอดที่ต้องชำระ
  2. สรุปยอดชำระรายบุคคล

     * แสดงยอดที่เล่น, ยอดค้าง, ชำระวันนี้ (เงินสด/โอน/ค้างจ่าย)
  3. ค้นหายอดค้างชำระ

     * ดึงข้อมูลจาก `OSpayment.txt`
  4. สรุปข้อมูลรายวัน (ส่งข้อมูลต่อให้เมนู 3)
  5. เปลี่ยนราคาลูกขนไก่
  6. เปลี่ยนราคาสนามต่อคน

---

### 🔹 3. สรุปข้อมูล (`menu3.c`)

ระบบสรุปผลการเล่นในแต่ละวันหรือแต่ละเดือน

* `1. สรุปข้อมูลรายวัน`

  * ถ้ามีไฟล์สรุปใน `output/` จะแสดงจากไฟล์นั้นทันที
  * ถ้ายังไม่มี จะอ่านไฟล์ใน `input/` แล้วสร้างไฟล์สรุปใหม่
  * มีให้เลือก 2 แบบ:

    * สรุปแบบ **ละเอียด (พร้อมรายชื่อ)**
    * สรุปแบบ **ย่อ (ไม่มีรายชื่อ)**

* `2. สรุปข้อมูลรายเดือน`

  * รวมข้อมูลจากไฟล์รายวันใน `input/`
  * แสดงยอดรวมลูก, ผู้เล่น, รายได้, เงินสด/โอน, และยอดค้างจ่ายจาก `OSpayment.txt`

---

## 📄 รูปแบบไฟล์สรุป (ภาษาไทย)

ตัวอย่างไฟล์ `output/07-11-2025.summary.full.txt`
(มี BOM รองรับภาษาไทยใน Notepad)

```
วันที่|07-11-2025
ลำดับ|ชื่อเล่น|เลขสมาชิก|จำนวนลูก|ยอดวันนี้|ชำระวันนี้|ชำระค้าง|วิธีชำระ
1|บาส|3|10|50|50|0|เงินสด
2|ฝน|2|8|40|40|0|โอน
3|ชาย|1|5|25|0|25|ค้างจ่าย
รวมจำนวนลูกทั้งหมด|23
ค่าเฉลี่ยลูก/คน|7
ยอดรับรวมทั้งหมด|115
```

---

## 💾 ไฟล์สำคัญ

| ไฟล์            | หน้าที่                        |
| --------------- | ------------------------------ |
| `member.txt`    | เก็บข้อมูลสมาชิก               |
| `OSpayment.txt` | บันทึกยอดค้างชำระ              |
| `config.txt`    | ราคาลูกและค่าสนาม              |
| `input/*.txt`   | ข้อมูลรายวัน                   |
| `output/*.txt`  | รายงานสรุป (ภาษาไทย UTF-8 BOM) |

---

## 🧱 การเพิ่มฟังก์ชันใหม่

หากต้องการขยายระบบ เช่น เพิ่มระบบ “จัดอันดับผู้เล่น”
สามารถสร้างไฟล์ `menu4.c / menu4.h` แล้ว include ใน `main.c` ได้ทันที:

```c
#include "menu4.h"
```

---

## 🧠 เทคนิคการพัฒนา

* ใช้ `fopen` แบบ `"a"` เพื่อ append ข้อมูลลงไฟล์
* ใช้ `strftime` / `time()` เพื่อบันทึกวันที่สมัครอัตโนมัติ
* ใช้ `fwrite("\xEF\xBB\xBF",1,3,fp)` เพื่อให้ไฟล์ภาษาไทยอ่านใน Windows ได้ถูกต้อง
* ใช้ `struct` และ `enum` เพื่อจัดกลุ่มข้อมูลให้ชัดเจน (เช่น `Member`, `DailyEntry`, `PaymentType`)

---


## 📜 License

โปรเจกต์นี้จัดทำเพื่อการศึกษา
สามารถนำไปต่อยอดหรือปรับใช้ได้ โดยให้เครดิตผู้พัฒนาเดิม

---
